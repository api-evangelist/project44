naftiko: 1.0.0-alpha2
info:
  label: project44 Tracking API — Status
  description: 'project44 Tracking API — Status. 1 operations. Lead operation: Get shipment status updates. Self-contained
    Naftiko capability covering one Project44 business surface.'
  tags:
  - Project44
  - Status
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PROJECT44_API_KEY: PROJECT44_API_KEY
capability:
  consumes:
  - type: http
    namespace: tracking-status
    baseUri: https://api.project44.com/api/v4
    description: project44 Tracking API — Status business capability. Self-contained, no shared references.
    resources:
    - name: shipments-shipmentId-status-updates
      path: /shipments/{shipmentId}/status-updates
      operations:
      - name: getshipmentstatusupdates
        method: GET
        description: Get shipment status updates
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: limit
          in: query
          type: integer
    authentication:
      type: bearer
      token: '{{env.PROJECT44_API_KEY}}'
  exposes:
  - type: rest
    namespace: tracking-status-rest
    port: 8080
    description: REST adapter for project44 Tracking API — Status. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/shipments/{shipmentid}/status-updates
      name: shipments-shipmentid-status-updates
      description: REST surface for shipments-shipmentId-status-updates.
      operations:
      - method: GET
        name: getshipmentstatusupdates
        description: Get shipment status updates
        call: tracking-status.getshipmentstatusupdates
        with:
          limit: rest.limit
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: tracking-status-mcp
    port: 9090
    transport: http
    description: MCP adapter for project44 Tracking API — Status. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: get-shipment-status-updates
      description: Get shipment status updates
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: tracking-status.getshipmentstatusupdates
      with:
        limit: tools.limit
      outputParameters:
      - type: object
        mapping: $.
